# Axia Backend Exam - REST API

This is a RESTful API built with **Node.js**, **Express**, and **MongoDB (Atlas)** for managing users. It supports user registration, login, and basic CRUD operations.

## 📁 Project Structure

